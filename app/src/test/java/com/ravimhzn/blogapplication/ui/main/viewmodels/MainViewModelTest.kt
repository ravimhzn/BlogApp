package com.ravimhzn.blogapplication.ui.main.viewmodels

import org.junit.Assert.*

import org.junit.Assert.assertEquals
import org.junit.Before
import org.junit.Test
import org.mockito.Mock
import org.mockito.Mockito
import org.mockito.MockitoAnnotations

import com.nhaarman.mockitokotlin2.argumentCaptor
import com.nhaarman.mockitokotlin2.verify
import com.nhaarman.mockitokotlin2.whenever

class MainViewModelTest {

    //Begin region for constants

    //End region for constants

    //Begin region for helper fields

    //End region for helper fields


    private var SUT: MainViewModel? = null

    @Before
    fun init() {
        MockitoAnnotations.initMocks(this)
        SUT = MainViewModel()

    }

    //Begin region for helper methods

    //End region for helper methods

    //Begin region for helper classes

    //End region for helper classes
}